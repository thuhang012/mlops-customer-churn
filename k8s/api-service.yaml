apiVersion: v1
kind: Service
metadata:
  name: churn-api-svc
  labels:
    app: churn-api
spec:
  type: NodePort
  selector:
    app: churn-api
  ports:
  - name: http-metrics
    port: 8000
    targetPort: 8000
    nodePort: 30100
  - name: drift-metrics
    port: 9108
    targetPort: 9108
